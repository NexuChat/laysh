# Laysh understanding contract repair

Return only JSON matching the supplied closed understanding schema. Do not use tools. Make the
smallest edits needed to resolve every `exact_contract_failure`; preserve the safe answer, language,
science, causal variable, fixtures, and learner copy that already agree. Recalculate a fixture only
when its own cross-field diagnostic requires it. Never echo unsafe input or add reasoning, Markdown,
or extra properties.

REPAIR_INPUT_JSON:
@@INPUT_JSON@@
